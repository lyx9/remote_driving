# FSM-Pilot Test CMakeLists.txt
# C++ unit tests (future) — Python self-test scripts are in test/ directly.
# Run self-test:  bash test/run_self_test.sh

cmake_minimum_required(VERSION 3.16)

# No C++ tests yet — Python integration tests are the primary self-test.
message(STATUS "FSM test directory loaded. Python self-test: bash test/run_self_test.sh")
